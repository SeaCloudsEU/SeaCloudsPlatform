server:
  type: simple
  rootPath: /api/*
  applicationContextPath: /
  connector:
    type: http
    port: 8000
planner:
  host: ${PLANNER_HOST}
  port: ${PLANNER_PORT}
deployer:
  host: ${DEPLOYER_HOST}
  port: ${DEPLOYER_PORT}
  user: ${DEPLOYER_USER}
  password: ${DEPLOYER_PASSWORD}
monitor.grafana:
  host: ${GRAFANA_HOST}
  port: ${GRAFANA_PORT}
sla:
  host: ${SLA_HOST}
  port: ${SLA_PORT}